export default function OfflinePage() {
  return (
    <div className="screen">
      <h1 className="brand-title">Offline</h1>
      <p>The counting app shell is cached on this tablet. Open Yard Receiving or Shipping from Home after you have used them once so they stay available without a signal.</p>
      <p>Counts tap-saved on this device will sync when the tablet is back online.</p>
    </div>
  );
}
